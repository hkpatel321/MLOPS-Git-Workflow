// Global Application Configuration
const config = {
    appName: "TeamDemoApp",
    version: "1.0.0",
    timeout: 30, // Note: This is the line that will cause the conflict
    environment: "development"
};

module.exports = config;